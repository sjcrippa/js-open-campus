//NOTACION

// ; = delimitar el final de una linea.
const b = 4;

b + 4;

// . = se utiliza en los objetos para acceder a los atributos
// movil.anchura

// [] = listas, arreglos o arrays
const ar = [1, 2, 3, 4]
console.log (ar[2])

// () = Funciones
function suma (a, b) {
    // se escribe la funcion
}

// {} = Llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10, 
}

if (true){
    // va a pasar todo lo que haya entre llaves

    const constante2 = "hola"
}